# CLAUDE.md — 별밤책 작업 안내

> 클로드가 이 저장소에서 작업할 때 먼저 읽는 파일.

## 이 프로젝트
**별밤책** — 엄마·아빠 목소리로 읽어주는 아기 잠자리 그림책 (PWA). 순수 HTML/CSS/JS, 빌드 과정 없음.
녹음·저장·재생은 **이 기기 안에서만**(IndexedDB, 서버 없음).
공개 주소(하나뿐): **https://minjung9767-gif.github.io/voice-book/** — GitHub Pages.

## 🚨 제일 중요
- **민정은 비개발자**다. 답변은 **한국어로, 쉽게, 간결하게**. 전문용어는 비유로 풀어서.
- **폰(아이폰)에서** claude.ai/code로 작업하는 경우가 많다.
- 기본은 **상의·설명**. 코드 수정은 **"고쳐줘/해줘"라고 명시할 때만** 한다.
- 애매하거나 구조에 영향 있는 결정은 **먼저 물어본다**.

## 📝 "나 뭐 해야 돼?" 라고 물으면
→ **`ROADMAP.md`** 를 열어서 **📌 민정이 할 일** 항목을 쉬운 말로 알려준다.

## 작업 방식
- 새 브랜치 → 커밋 → PR → **머지(squash)까지 클로드가 한다**. 머지되면 **GitHub Pages 자동 배포**.
  (넷리파이는 처음에 써 보다가 **안 쓰기로 했다** — 자세한 건 ROADMAP '넷리파이' 항목.)
  - 민정이 PR 링크만 받고 머지를 못 해서 "앱이 안 바뀐다"고 헤매는 일이 있었다. **PR만 만들고 끝내지 말 것.**
  - **이슈가 있으면 머지하지 말고 먼저 보고**한다(충돌·동작 이상·애매한 결정 등). 쉬운 말로 설명하고 상의한다.
  - 이 저장소엔 자동 검사(CI)가 없다 → 머지 전에 **직접 브라우저로 띄워 확인**하는 것으로 대신한다.
- 앱을 크게 바꾸면 `sw.js`의 `CACHE` 버전을 올린다(폰에 바로 반영, network-first).
- 확인은 브라우저로 직접 띄워서(로컬 `python3 -m http.server`), 마이크는 https/localhost에서만 동작.

## 코드 지도
| 파일 | 역할 |
|---|---|
| `index.html` | 화면 뼈대(홈·이야기·보기·모달) |
| `style.css` | 밤 테마 스타일 |
| `app.js` | 녹음/저장(IndexedDB)/재생/이름/백업/도움말/의견 로직 |
| `scripts-data.js` | 동화 대본 (여기에 이야기 추가) |
| `analytics.js` | 사용 통계(GoatCounter) |
| `manifest.webmanifest`, `sw.js` | PWA(홈화면 앱·오프라인) |
| `icons/` | 앱 아이콘 (`gen_icons.py`로 재생성) |
| `ROADMAP.md` | 할 일 메모 · 주소/계정 정보 |
