# 별밤책 — 로드맵 · 작업 메모

"나중에 할 것들"과 "이어서 할 작업"을 잊지 않으려고 적어두는 파일.

_마지막 갱신: 2026-08-05 · 현재 버전 v18_

## 이름
- **현재 앱 이름: 별밤책** (밤 버전)
- **낮 버전(자매품) 예정 이름: 햇살책** ← 나중에 낮 버전 만들 때 사용
  - (후보였던 것: 낮잠책, 해뜰책)

---

## 🔜 다음에 이어서 할 작업 (피드백 라운드)

각 항목에 문제·손댈 파일·방법 후보·결정할 점을 적어 바로 이어서 작업할 수 있게 함.

### B1. 대본 문단·띄어쓰기 정리 (읽기 쉽게 끊어읽기) ⬅️ 우선순위 높음
- **문제:** 지금은 모든 줄이 `.line`에 **똑같은 16px 간격**이라 장면 구분이 없어 "다 이어져" 보임. 어디서 끊어 읽을지 헷갈림.
- **원하는 것:** 장면(문단)별 간격 다르게, 대사/지문 묶기, 긴 문장을 읽기 좋은 호흡으로 다시 나누기.
- **손댈 곳:** `scripts-data.js`(대본 5편 재구성), 필요시 `style.css .line`(문단 간격) / `app.js renderScriptBody`.
- **방법 후보:** ① 빈 줄/구분자로 문단 나누기(최소 변경), ② 각 이야기를 `paragraphs`(줄 배열의 배열) 구조로 바꿔 문단 간격 크게·문단 내 간격 작게.
- **결정 필요:** 데이터 구조 변경(②) vs 최소 변경(①).

### B3. 녹음 중간 실수 → 처음부터 다시 하는 문제 개선
- **문제:** 한 번에 통으로 녹음. 중간에 틀리면 처음부터.
- **방식 후보 (미정):** ① 일시정지·이어녹음(MediaRecorder `pause()/resume()`, 쉬움) · ② 문단별 녹음 후 자동 이어붙이기(편하지만 큼, B1과 묶으면 자연스러움) · ③ 녹음 전 3초 카운트다운.
- **손댈 곳:** `app.js` 녹음 로직(`startRecording`/`onRecorderStop`/도크).
- **결정 필요:** ①~③ 중 무엇으로.

### B4. 백업 UX 용어·안내 정리
- **문제:** "⬇ 내려받기"(오디오 파일 사본)와 "🛟 녹음 백업"(전체 JSON, 복원 가능)이 이름이 비슷해 헷갈림. 내려받기 파일은 **복원으로 못 되살림**(복원은 JSON만).
- **원하는 것:** 용어 구분 명확히, 복원 안내를 더 친절하게(가능하면 단계/그림).
- **손댈 곳:** `app.js`(`downloadSaved` 라벨, `openBackup` 모달 문구). *카톡에서 가져오는 법은 v16에서 한 줄 추가함 → 더 다듬기.*

### B5. 저장 위치 파편화(인앱 브라우저) 대응 ⚠️ 크리티컬
- **문제:** 녹음은 IndexedDB에 **브라우저별로 따로** 저장. 카카오톡 인앱 브라우저에서 녹음 → 나중에 사파리로 열면 **안 보임**(웹앱 근본 한계).
- **지금 상태:** 인앱 브라우저면 "사파리로 열어주세요" 경고 모달(`app.js showIabNoticeIfNeeded`).
- **원하는 것:** 경고 강화/친절하게, 백업 유도 쉽게.
- **근본 해결책:** 아래 "클라우드 백업"(선택 로그인).
- **결정 필요:** 단기(경고 강화)만 vs 중기(클라우드 백업) 착수.

---

## 나중에 검토할 것 (장기)
- **클라우드 백업(선택 로그인)** — 녹음을 인터넷에 안전 보관해 폰을 바꾸거나 데이터를 지워도 되살아나게. 서버·비용 필요. 프리미엄(수익화)과 연결. (B5의 근본 해결책)
- **낮 버전(햇살책)** — 낮에 보는 밝은 테마 + 활기찬 이야기.
- **동화 더 추가** — 전래동화 등.
- **수익화** — 반응 좋아지면 구독/동화팩 등 검토.

---

## ✅ 최근 완료 (v16~v18)
- 오른쪽 위 달 은은하게 · 빨간 모자 "빵과 우유→따뜻한 죽" · 홈 하단/의견 문구 정리 · '파일 불러오기' 버튼 제거 · 백업 복원 안내에 카톡 가져오기 한 줄 추가 (v16)
- 홈 화면 아이콘: 마이크 → 동화책 표지(밤하늘 + 큰 초승달·별 + 페이지 결) (v18)
- (이미 되어 있던 것: 추천 이름 "별밤책", 도움말 홈 화면 추가 안내)

## 지금 방식 요약 (참고)
- 녹음·저장·재생은 이 기기 안에서만(IndexedDB). 서버 없음.
- 배포: Netlify(정식 `voice-book.netlify.app`) + GitHub Pages(백업).
- 사용 통계: GoatCounter(`voicebook.goatcounter.com`), 익명.
- 피드백: 앱 안 폼 → Netlify Forms → 메일 알림.
- 파일/asset 크게 바꾸면 `app.js APP_VERSION` 과 `sw.js CACHE` 버전 숫자 올리기(폰 반영용).
- 아이콘 다시 만들기: `cd icons && python3 gen_icons.py` (Pillow 필요). 아이폰은 이미 추가한 홈 화면 아이콘이 자동 갱신 안 됨 → 삭제 후 재추가.
