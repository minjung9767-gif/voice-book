# 별밤책 — 할 일 메모 📝

> 민정이 "나 뭐 해야 돼?" 하고 물으면, 클로드는 이 파일을 열어보고 **📌 민정이 할 일**부터 알려준다.
> (엄마·아빠 목소리로 읽어주는 아기 잠자리 그림책 앱)

---

## 📌 민정이 할 일 (지금 남은 것)

- [ ] **0. 새 버전(v24 · 한 목소리 · 아기 홈) 폰에서 확인** ⬅️ 제일 먼저
  - 홈이 **그림 카드 2열**로 바뀌었는지, **녹음 전 이야기는 흐릿한지**(자리는 그대로)
  - **🌙 쭉 들려주기** 눌러서 계속 이어지는지, 한 편 끝나면 **다른 이야기로 저절로 넘어가는지**
  - 읽는 중에 **왼쪽 위 🏠 홈 버튼**이 계속 보이는지
  - 녹음은 홈 맨 아래 **🎙 녹음하기** → 이야기 고르기 → 녹음
  - ⭐ **음질 확인:** 한 장면 **다시 녹음**해서 예전 것과 비교. 지지직이 줄었는지.
    (조용한 방에서 · 마이크는 폰 아래쪽 · 입에서 **한 뼘** 정도)

- [ ] **1. 새 버전(v23 · 전면 개편) 폰에서 확인** — 라이브 = **minjung9767-gif.github.io/voice-book/**. 홈 → 이야기 → **장면마다 녹음** → 다 하면 홈에서 눌러 **들려주기**(그림이 목소리 따라 넘어가는지). 예전에 녹음해 둔 게 있으면 `예전 녹음` 으로 뜨는지도. 아이폰 홈 화면 앱은 **삭제 후 재추가**해야 새 아이콘이 보임.
- [ ] **2. 백업 실기기 테스트** — 새 버전에서 **💬 카카오톡으로 백업 보내기** → 카톡 '나에게'에 `별밤책-백업.txt` 도착하는지, 그걸로 **복원**까지 되는지 확인. (이게 이번 핵심 수정)
- [ ] **3. 의견 폼 마무리 3가지** (구글 폼 연결은 끝남 ✅)
  - [ ] 폼 **이름 바꾸기**: `제목 없는 설문지` → **`별밤책 의견`** (드라이브에서 찾기 쉽게)
  - [ ] **이메일 알림 켜기**: 폼 → **응답** 탭 → 오른쪽 ⋮ → **새 응답에 대한 이메일 알림 받기**
  - [ ] **배포 후 실기기 테스트**: 앱에서 의견 하나 보내보고 → 구글 시트에 줄이 생기는지 확인 (안 생기면 클로드에게 말하기)
- [ ] **4. GoatCounter 이메일 인증** — 통계 계정(voicebook.goatcounter.com) 가입 때 온 인증 메일 링크 안 눌렀으면 눌러두기.
- [ ] **5. 주변 부모들한테 공유하고 반응 보기** — 홈 아래 **`🎁 공유`** 버튼을 누르면 소개 글 + 주소가 한 번에 나간다(v30).
  - ⭐ 주소는 **https://minjung9767-gif.github.io/voice-book/** 하나뿐. **바뀌지 않는다.**
    (구)`voice-book.netlify.app` 은 크레딧 소진으로 **v18에서 멈춰 있음** → 그 주소를 열면 옛 화면만 보인다.
    혹시 홈 화면 앱이 옛 주소로 추가돼 있으면 **백업 → 삭제 → 새 주소로 재추가**.
  - 공유 글에 "사파리·크롬으로 열어주세요" 안내가 이미 들어 있다.

---

## 🎨 긴주 스타일 재디자인 (진행 중 — 시범 `story-demo.html`)

긴주 놀이터 '잠자리 동화' 화면 느낌을 별밤책에 이식 중. 시범 페이지에서 검증 후 본 앱 이관.
- 시범 주소: `minjung9767-gif.github.io/voice-book/story-demo.html`
- 참고 원본: `github.com/minjung9767-gif/ginjoo-play-program` (js/games/story.js, css/style.css)

### ▶ 새 세션에서 이어가기: **지금 상태 = "시범 완성 → 전면 개편 착수" 직전**
- 시범(`story-demo.html`)에서 **녹음+확인 화면**은 검증 끝. 다음은 **전면 개편**(아래 계획).
- 아이콘은 **보류**(방향만 정함, 맨 아래 참고).

**확정(시범 `story-demo.html`에서 검증 완료):**
- 밤 테마 + `Jua`/`Dongle` 폰트, 장면(이모지+문장) 구조.
- **대본 5편 전부 장면화 완료**(원문 그대로 + 이모지). 상단 **임시** 이야기 전환(‹ 제목 ›)으로 5편 오감.
- **장면별 클립 녹음**(엄마/아빠 각각, 아무 장면이나 다시). 저장: IndexedDB `voicebook-demo`(본 앱과 분리), 키 `storyId:v2:voice:scene`.
- **녹음 화면 = 부모 '녹음+확인' 단일 모드** (아기용 자동재생/들려주기는 이 화면에 **없음**):
  - 아래 가운데 **페이저(❮ n/N 장면 ❯)** — 녹음 중에도 유지(화살표만 비활성)
  - 그 위 **'🔊 녹음 확인'**(녹음된 장면일 때만; 재생↔멈추기 토글, 확인 중 ❯ 누르면 다음 녹음 이어듣기)
  - 맨 아래 **고정** '🔴 이 장면 녹음 / 다시 녹음'
  - 옆 화살표 제거 → **글 폭 넓힘**(문장 덜 쪼개짐, 글씨 크기 일정)

**본 앱 이관 시 반영 (긴주와 다르게):**
- **홈 버튼**: 긴주는 바깥 앱으로 나가지만, 별밤책은 단일 앱 → 홈 버튼은 **별밤책 메인(이야기 고르기) 화면으로**.
- **오른쪽 상단 음소거 버튼 제거** — 불필요.
- (참고) 긴주 최근 변경: 다음 이야기 버튼 삭제 · 이모지 톤다운 · '책장' 표현.
- ※ 긴주에 민정이 이슈로 적어둔 메모가 더 있으면, 이 세션은 voice-book만 접근 가능 → 그 내용은 채팅에 붙여주면 반영.

**➡️ 다음 단계: 전면 개편 (방식 = "시범을 키워서 → 완성되면 메인 교체")**
살아있는 `index.html`을 바로 갈아엎지 말고, **`story-demo.html`을 완전한 앱으로 키운 뒤** 다 되면 메인으로 교체. (안전 · 미리보기 유지 · 마지막 1회 교체)

빌드 순서:
1. ✅ **이야기 고르기(홈) — 세로 목록** *(완료: `story-demo.html`)*. picker는 별밤책이 단일 앱이라 그리드 대신 **세로 목록**으로 결정. 각 줄 = 이모지 표지 + 제목 + 엄마🌷/아빠🌰 녹음 진행(n/N). 이야기 누르면 녹음+확인 화면 진입, 화면 위 🏠 홈 버튼으로 목록 복귀(임시 '‹ 제목 ›' 전환은 제거).
2. ✅ **아기 자동재생(들려주기) 화면** *(완료: `story-demo.html`)*. **라우팅 규칙 확정:** 홈에서 이야기 탭 → 한 목소리라도 **전체(전 장면) 녹음 완료면 들려주기**, 아니면 녹음하기. 완성된 줄엔 오른쪽에 **🎙(녹음 고치기)** 따로 — 녹음 접근은 **홈에서만**, 아기 화면엔 녹음으로 가는 길 **전혀 없음**(실수로 못 날림). 들려주기 = 장면 클립 자동 이어재생 + 그림 동기화, 버튼 없음. 화면 탭 = 멈춤/이어보기, 멈추면 **🏠 목록·🔁 처음부터**(+엄마·아빠 둘 다 완성이면 목소리 전환)만. 끝나면 '다 읽었어요'.
   - ✅ **랜덤 재생 규칙 (v24에서 구현 완료):** 이야기 **단위**로 섞기(엄마/아빠 조합 단위 ❌ — 같은 내용 목소리만 바꿔 연달아 나오면 안 됨), 한 번 나온 이야기는 한 바퀴 돌기 전엔 다시 안 나옴, 목소리는 고른 쪽으로 통일, **완성된 이야기만** 후보.
3. ✅ **백업/복원(장면 클립 포맷)** *(완료: `story-demo.html`)*. 홈 우상단 **🛟 백업·복원** → 모달. 모든 장면 클립 → base64 `.txt` 하나(`{app,kind:"scene-clips",version:2,clips:[{key,mime,data}]}`), 공유(카톡 '나에게'), 복원 = **'합치기'**(같은 키 덮어쓰고 없던 키 추가). **부모끼리 파일 주고받아 복원하면 엄마·아빠 목소리가 한 폰에 합쳐짐**(mom 복원→아빠 복원→both 확인). 아이폰 gesture 대응(모달 열 때 파일 미리 생성).
4. ✅ **아기 이름·도움말·의견 얹기** *(완료: `story-demo.html`)*. 홈 하단 버튼 줄 = **✏️ 아기 이름 · 🛟 백업 · ❔ 도움말 · 💬 의견**. 이름 넣으면 대본 `{이름아}`에 반영(받침 조사 자동). 홈 버튼(메인으로)은 녹음/들려주기 화면에 이미 있음. 음소거 버튼은 데모에 원래 없음(뺄 것 없음). ⚠️ **의견은 기존 앱과 동일하게 Netlify Forms(`POST /`, form-name=feedback)** — GitHub Pages(현재 라이브)에선 실제 전송 안 됨, **Netlify 복구 + 메인 교체 후** 동작.
5. ✅ **메인(`index.html`)으로 교체 완료 (v23)**. 홈 구성은 v22의 **⚙️ 더보기 + 제목 위 아기 이름**을 그대로 유지. 숨은 Netlify 폼도 유지. 아래 "전면 개편 이관" 참고.

### ▶ 한 목소리 · 아기 홈 개편 (v24) — 실제 앱에 적용됨

민정 결정으로 아래를 한 번에 반영했다.

**1. 목소리 = 한 명** (같은 내용을 엄마·아빠 두 벌 녹음할 필요 없음)
- 화면에서 👩엄마/👨아빠 **탭이 사라짐**. 백업/복원 파일 형식은 그대로.
- 저장 열쇠는 **예전 그대로**(`id:v2:mom:n` / `id:v2:dad:n`) 두고, **이야기마다 한 자리만 쓴다**:
  `storyVoice()` = 장면이 **더 많이 녹음된 쪽**, 수가 같으면 **아빠**(민정 결정), 아무것도 없으면 `mom`.
- 안 쓰는 쪽 녹음도 **지우지 않는다**(백업에 그대로 남음). → 기존 녹음 손실 0.

**2. 홈 = 아기 화면** (부모용과 분리)
- **그림 카드 2열**, 순서는 **대본 순서 그대로 절대 안 바뀜**(아기가 자리로 기억하니까).
- 녹음 전 이야기는 **흐릿하게 자리만 지킴**. 누르면 "아직 녹음 전이에요" 안내만.
- 홈 맨 아래 **`🎙 녹음하기` · `⚙️ 더보기`** 두 버튼.
- **`🎙 녹음하기` → 새 화면(`#pick`, 세로 목록 + 진행률) → 녹음 화면.** 녹음은 한 단계 안쪽에만 있다.

**3. 랜덤 이어듣기** (ROADMAP에 적어둔 규칙대로 구현)
- 홈 맨 위 **`🌙 쭉 들려주기`** — 아무거나 골라서 계속 이어 재생.
- 이야기를 직접 골라 들어도 **한 편이 끝나면 2.6초 뒤 다른 이야기로 자동**.
- 이야기 **단위**로 섞고, 한 바퀴 돌기 전엔 같은 이야기가 다시 안 나옴. **완성된 이야기만** 후보.
- 멈춤 화면: `🎲 다른 이야기` / 끝 화면: `🎲 지금 바로` · `■ 그만 들을래요`. (예전 `🔁 처음부터`는 없앰)

**4. 버튼 통일** — 긴주 놀이터와 같은 **동그란 흰색 아이콘 버튼**(`.icon-btn`).
- 들려주기 화면: **왼쪽 위 🏠**(`.corner-home`) — 읽는 중에도 계속 보임(멈출 필요 없음).
- 녹음 화면: 왼쪽 `‹`(이야기 고르기) · 오른쪽 `🏠`(홈).
- 홈 말고 다른 화면에서는 배경 **장식 달을 숨김**(`body.no-moon`) — 오른쪽 위 버튼과 겹쳐서.

**5. 녹음 음질 개선** ⭐ (민정: "폰으로 녹음하면 지지직거린다")
- 원인: 폰 브라우저가 마이크에 **통화용 처리**(에코 제거·소음 억제·자동 볼륨)를 기본으로 걸어서
  목소리를 실시간으로 깎는다. + 비트레이트를 지정 안 해 기본(낮은) 값으로 압축됐다.
- 조치: `getUserMedia`에서 `echoCancellation / noiseSuppression / autoGainControl` **모두 끔** +
  `audioBitsPerSecond: 128000`. 설정을 못 받는 기기는 예전 방식으로 자동 대체.
- ⚠️ 소음 억제를 껐으므로 **조용한 방**에서 녹음해야 한다. 안내 문구를 녹음 고르기 화면·도움말에 넣음.

### ▶ 백그라운드 재생 (v34) — 화면 꺼도 계속 들리게

민정 요청: 재우면서 **화면 끄고 소리만** 듣고 싶다.

**왜 안 됐나.** 장면마다 `new Audio()` 를 새로 만들어 틀고 있었다.
아이폰은 화면이 꺼진 동안 *"사람이 안 눌렀는데 새로 트는 것"* 을 막기 때문에,
**한 장면만 나오고 멈춘다**(11곡짜리 앨범을 한 곡씩 손으로 트는 구조였던 셈).

**고친 방법 두 가지.**
1. **재생기를 하나만 쓴다**(`getAudio()` / `audioEl`). 내용(`src`)만 갈아끼운다.
   그러면 "아까 사람이 튼 그것"으로 인정돼 화면을 꺼도 이어진다.
   `stopPlayAudio()` 도 **재생기를 버리지 않고** 소리만 멈춘다.
   > 🚨 `new Audio()` 를 장면마다 만드는 방식으로 되돌리지 말 것.
2. **Media Session** — 잠금화면·제어센터에 `이야기 제목 / ○○이의 별밤책 / n / N 장면` 과
   앱 아이콘, ⏯️·⏭️ 버튼을 띄운다. 음악 앱처럼 보여야 폰이 '진짜 재생 중'으로 대우해 준다.
   버튼: `play` `pause` `nexttrack`(다른 이야기) `previoustrack`(이 이야기 처음부터) `stop`(홈).
   재생/멈춤 상태(`playbackState`)도 늘 맞춰 준다.

**검증(브라우저).** 여러 장면이 지나가도 `Audio` 생성은 **1번뿐** ✅ /
잠금화면 정보 `커다란 순무 · 지우의 별밤책 · 8 / 11 장면 · 아이콘 2개` ✅ /
버튼 5개 등록 ✅ / 멈춤↔이어듣기에 따라 상태가 `paused`↔`playing` ✅

⚠️ **아이폰 실기기 확인은 민정 몫.** 여기서는 아이폰 사파리·홈화면 앱의 실제 동작을 검증할 수 없다.
폰·브라우저 버전에 따라 다를 수 있으므로, 안 되면 그 사실을 알려달라고 도움말에도 적어 두었다.

### ▶ 공유할 때 파일이 첨부 안 되던 문제 (v33) — 🚨 되돌리지 말 것

민정 제보: *"나는 txt 파일 보내기가 됐는데, 친구가 공유하기를 누르니 **카톡에 파일이 첨부가 안 돼서** 못 보냈다."*

**원인 — 파일과 글을 같이 보냈다.**
```js
navigator.share({ files: [file], title: "…", text: "…" })   // ❌ 카톡이 글만 받고 파일을 버린다
navigator.share({ files: [file] })                          // ✅ 파일만 보낸다
```
Web Share 로 `files` 와 `title`/`text` 를 함께 실어 보내면, 받는 앱(특히 카카오톡)이
**글만 처리하고 파일을 누락**하는 일이 있다. 기기·앱 버전에 따라 되기도 하고 안 되기도 해서
민정 폰에서는 됐지만 친구 폰에서는 안 됐다.

> 🚨 **`sendBackup` 에서 files 와 text 를 다시 함께 보내지 말 것.**

**같은 종류의 문제를 `shareApp` 에서도 고쳤다.** `text` 와 `url` 을 함께 보내면
앱에 따라 **주소만 가져가고 글을 버려서** "사파리·크롬으로 열어주세요" 안내가 통째로 사라진다.
→ 주소는 이미 글 안에 있으므로 **`text` 만** 보낸다.

**공유가 안 되는 환경 안내도 고쳤다(`backupFallback`).**
예전엔 파일만 슬쩍 저장하고 작은 토스트만 띄워서 사용자는 "아무 일도 안 일어났다"고 느꼈다.
이제 무슨 일이 있었는지와 다음에 뭘 하면 되는지를 창으로 보여준다 —
카톡·인스타 안이면 "사파리로 여는 법", 그 밖이면 "저장된 파일을 카톡에 첨부하는 법".
백업 힌트 줄에도 인앱 브라우저면 **미리** 경고한다.

**검증(4가지 환경).** 공유창 되는 폰 → 보낸 항목이 `["files"]` 하나뿐 ✅ /
알려주기 → `["title","text"]`, url 없음 ✅ / 카톡 안 → 사파리로 여는 법 안내 ✅ /
공유창 없는 기기 → 파일 저장 + 카톡 첨부하는 법 안내 ✅

### ▶ 홈 화면에 추가하기 버튼 (v32)

공유로 들어온 부모들이 브라우저로만 보고 나가는 걸 줄이려고, 홈 아래에 **`📲 홈 화면에 앱처럼 추가하기`** 띠를 넣었다.

| 기기 | 동작 |
|---|---|
| **안드로이드·크롬** | `beforeinstallprompt` 를 붙잡아 뒀다가 **바로 설치창**을 띄운다 |
| **아이폰·사파리** | ⚠️ 애플이 API를 안 열어줘서 **자동 추가가 불가능**하다 → 공유(⬆️) → "홈 화면에 추가" 방법을 번호로 안내 |

**띠를 감추는 경우** (`updateInstallBar`) — 하나라도 해당되면 안 보인다:
- 이미 홈 화면 앱으로 열림(`isStandalone`) 또는 설치 완료 기록(`localStorage.installed`)
- 카톡·인스타 등 **인앱 브라우저** (추가가 안 되고, 이미 "사파리로 열어주세요" 안내가 뜬다)
- 설치 기회도 없고 아이폰도 아님 (PC 등)

⚠️ `isStandalone` / `isIOS` 는 **유틸 맨 앞으로 옮겼다.**
`beforeinstallprompt` 가 일찍 발생하면 `updateInstallBar` 가 선언 전에 불려 오류가 나기 때문.

**검증(5가지 기기 흉내).** 아이폰 사파리 → 띠 + 안내창 ✅ / 안드로이드 → 설치창 바로 ✅ /
홈 화면 앱 → 띠 숨김 ✅ / 카톡 안 → 띠 숨김 ✅ / PC → 띠 숨김 ✅

### ▶ 지난 녹음 되돌리기 (v31) — 🛟 "다시 녹음했는데 예전 게 더 좋았다"

민정 질문: *"녹음했는데 예전 버전이 더 맘에 들면 어떡하지?"*
**v30까지는 다시 녹음하면 예전 녹음이 그 자리에서 사라졌다.** 되돌릴 방법이 없었다.

**방식 — 다 쌓되, 적당히.**
- 다시 녹음하면 예전 것을 지우지 않고 **`본래열쇠#h시각`** 으로 옮긴다(`pushHistory`).
- 장면마다 **최근 `HISTORY_KEEP`(=3)개**까지만 남기고 오래된 건 조용히 정리한다.
  ⚠️ 무제한으로 쌓으면 백업 파일이 몇 배가 되고, **아이폰이 백업 파일을 만들다 버거워진다.**
  정작 제일 중요한 백업이 못 쓰게 되는 쪽이 더 위험하다고 판단.
- 녹음 화면에 **`↩️ 지난 녹음 (n개)`** — **다시 녹음한 장면에만** 나타난다.
  열면 "방금 전 · 어제 · 3일 전"으로 뜨고, ▶로 들어본 뒤 `이걸로`를 누르면 **맞바꾼다**
  (지금 쓰던 것도 이력으로 들어가 다시 되돌릴 수 있다).
- 백업은 **기본으로 최신만** 담는다. 더보기에 **`☐ 지난 녹음까지 함께 담기`** 체크박스.
  백업 힌트에 **대략 몇 MB인지**도 같이 보여준다.

**열쇠 규칙 주의.** `#h` 가 붙으면 `loadProgress` 의 `split(":")` 검사에서 장면 번호가 숫자가 아니게 되어
**자동으로 걸러진다** → 진행률·완성 판정에 안 섞인다. 이 성질에 기대고 있으니 열쇠 형식을 바꾸지 말 것.

**검증.** 1번 장면을 네 번 녹음한 상태를 만들어 확인 —
버튼에 `(3개)` 표시 ✅ / 3차를 고르면 지금 녹음이 3차로 바뀌고 4차는 이력으로 들어감 ✅ /
가장 오래된 1차는 3개 제한으로 정리됨 ✅ / 다시 열어 되돌릴 수 있음 ✅ /
백업 개수 기본 1개 → 이력 포함 3개 ✅

### ▶ 공유하기 버튼 (v30)

주변 부모들에게 앱을 알릴 수 있게 홈 하단에 **`🔗 알려주기`** 를 넣었다
(`🔗 알려주기 · 🎙 녹음하기 · ⚙️ 더보기`). *(v31에서 🎁 → 🔗 로 바꿈 — 선물 이모지가 안 어울린다는 의견)*

- 누르면 폰 **공유창**(`navigator.share`) → 카톡·메시지 등으로 보낸다.
  공유창이 없으면 **주소 복사**, 복사도 막히면 **글을 띄워 직접 복사**하게 한다(3단 대비).
- 아이폰은 **누르자마자** 공유창이 떠야 해서, `shareApp()` 앞에 `await` 를 두지 않는다.
  (백업 버튼에서 겪었던 gesture 문제와 같은 이유 — 이 구조를 바꾸지 말 것)
- 사용자가 공유를 **취소**하면(`AbortError`) 조용히 넘어간다. 토스트도 안 띄운다.
- 통계: `share` / `share_copy` / `share_manual`.

> ⚠️ **공유 글 마지막 줄(사파리·크롬으로 열어달라는 안내)을 빼지 말 것.**
> 카톡으로 받아 카톡 안에서 열면 녹음이 **다른 곳에 저장돼** 나중에 사라진 것처럼 보인다(B5 문제).
> 문구는 `app.js` 맨 위 `SHARE_TEXT` / `SHARE_URL`.

### ▶ 동화 두 편 교체 (v29)

민정 요청 — **아기돼지 삼형제 → 🥕 커다란 순무**, **미운 아기 오리 → 🧤 장갑**.
둘 다 **반복 구조**라 아기가 따라 하기 좋다("영차, 영차!" / "나도 들어가도 돼?" - "그럼, 들어와!").
장갑은 마지막이 다 같이 포근해지는 장면이라 잠자리 책과 잘 맞는다.

- **자리는 그대로.** 대본 배열의 1번·3번 칸에 그대로 끼워 넣었다 → 홈 그림 카드 위치가 안 바뀐다
  (아기가 자리로 기억하기 때문. v24에서 정한 원칙).
- **새 id 를 썼다**(`big-turnip`, `mitten`). 예전 id 를 재사용하면 예전 녹음이 새 대본의
  엉뚱한 장면에 붙는다. ⚠️ 앞으로 이야기를 갈아끼울 때도 반드시 새 id 를 쓸 것.
- 뺀 두 편의 녹음은 **지우지 않는다**. 화면에서만 안 보이고 백업 파일엔 그대로 담긴다.

### ▶ 백업 주고받기 안전장치 (v28) — 🛟 녹음 유실 방지

민정 질문: *"엄마·아빠가 백업을 주고받을 때 케이스가 다양할 텐데 지금은 어떻게 되어 있나?
녹음 파일 날아가는 게 가장 치명적이다."*

**v27까지의 동작:** 복원은 키 기준 **무조건 덮어쓰기** + 없던 건 추가 + 내 것 중 파일에 없는 건 그대로.
엄마 칸/아빠 칸은 키가 달라서 **주고받기 자체는 안전**했지만, 구멍이 둘 있었다.

| 구멍 | 무슨 일이 나나 |
|---|---|
| ⚠️ 아빠가 **엄마 칸**에 녹음 | 기본값이 `mom` 이라 목소리를 안 바꾸면 엄마 칸에 담김 → 복원 시 엄마 녹음이 덮어써져 **사라짐** |
| ⚠️ **오래된 백업**을 나중에 복원 | `createdAt` 을 안 봐서, 어제 백업을 오늘 복원하면 오늘 녹음이 **되돌아감** |

**막은 방법 (셋 다 적용).**
1. **이 폰은 누구 폰** (`localStorage.myVoice`) — 녹음하러 처음 들어갈 때 한 번 묻고,
   새 녹음은 늘 그 사람 칸으로. 더보기에서 언제든 바꿀 수 있다. → 구멍 1 봉쇄.
2. **더 새것만 남기기** — 복원 시 같은 키면 `createdAt` 비교, **내 것이 최신이면 건드리지 않는다.** → 구멍 2 봉쇄.
3. **복원 미리보기** — 넣기 전에 `새로 들어와요 N / 더 새것으로 바뀌어요 M / 내 것이 최신이라 그대로 K` 를
   보여주고 확인받는다(`.plan`). 백업 파일 만든 날짜도 함께 표시.

**원칙(바꾸지 말 것): 복원은 '합치기'다. 어떤 경우에도 내 녹음을 지우지 않는다.**

**검증(실제 두 폰을 흉내내 확인).**
① 아빠가 2개 녹음 → 엄마 복원: 엄마 6개 그대로 + 아빠 2개 추가 ✅
② 엄마가 합친 걸 아빠에게 되돌려 보냄: 아빠 2개 그대로(최신) + 엄마 6개 추가 ✅
③ 아빠가 오늘 다시 녹음한 뒤 **어제 백업을 또 복원**: 오늘 녹음이 안 되돌아감 ✅

### ▶ 목소리 배지 · 순서대로 녹음 · 달/버튼 손질 (v27)

- **🎤 누구 목소리인지 배지.** 홈 그림 카드와 녹음 목록 둘 다.
  긴주는 대본에 `voice:"엄마"` 를 적어두는 방식이지만, 별밤책은 직접 녹음하므로
  **저장 칸(mom/dad)이 곧 라벨**이다 → 예전 녹음에도 자동으로 붙는다(다시 녹음 불필요).
  색: 엄마=분홍(`.gvoice`), 아빠=파랑(`.gvoice.dad`).
- **녹음 화면에 "누구 목소리?" 고르기 부활**(`.vtab`). 들어가면 그 이야기에 담긴 쪽이 자동 선택되고,
  틀렸으면 바꿀 수 있다. 이야기 하나 = 한 사람 원칙은 그대로.
- **녹음 후 이동을 '순서대로'로.** 예전엔 *아직 안 한 장면*으로 건너뛰어서,
  1번을 다시 녹음하면 6번으로 튀어 2·3번을 이어 다시 담을 수 없었다(민정 지적).
  시끄러운 데서 녹음해 **처음부터 다시 담고 싶은 경우**가 실제 니즈다.
- **🌙 달을 초승달로.** 꽉 찬 원이라 밤 배경 위에서 '회색 동그라미'처럼 칙칙했다.
  `box-shadow: inset` 으로 한쪽을 파내고 크림빛으로. 앱 아이콘·이름과도 맞는다.
- **동그란 아이콘 버튼(홈·뒤로) 또렷하게.** 배경 .14→.26, 테두리 .22→.5,
  아이콘에 그림자, 들려주기 화면의 `opacity:.5` 제거.

### ▶ 반쪽 섞임 완전 차단 (v26) — 🚨 새 버전 낼 때 반드시 지킬 것

v25에서도 또 안 보였다. 이번엔 **모양(style.css)만 예전 것**이 와서, 목록 칸이 납작해지고
`🎙 녹음하기 · ⚙️ 더보기` 버튼이 화면 위쪽으로 올라붙었다(민정 스크린샷).
v25의 자가 복구는 **DOM 요소 유무만** 봤기 때문에, 뼈대는 새것이라 이 경우를 못 잡았다.

**1차 방어 — 주소에 버전 붙이기 (진짜 해결책).**
`index.html` 이 `style.css?v=26` / `app.js?v=26` / `scripts-data.js?v=26` 처럼 부른다.
버전이 바뀌면 **주소 자체가 달라지므로** 브라우저가 가진 예전 사본이 끼어들 수 없다.

> 🚨 **새 버전을 낼 때는 아래 네 곳을 반드시 같이 올린다. 하나만 빠지면 앱이 계속 복구를 시도한다.**
> | 파일 | 고칠 곳 |
> |---|---|
> | `index.html` | `<html data-v="v26">` + 모든 `?v=26` 주소 |
> | `style.css`  | `:root { --v: "v26" }` |
> | `app.js`     | `APP_VERSION = "v26"` |
> | `sw.js`      | `CACHE = "voicebook-v26"` + `CORE` 안의 `?v=26` |

**2차 방어 — 판 대조 자가 복구(안전망).**
`versionsMatch()` 가 **뼈대(data-v) · 모양(--v) · 로직(APP_VERSION) · 대본(scenes 구조) · DOM 자리**를
한꺼번에 확인한다. 하나라도 어긋나면 캐시를 비우고 서비스워커를 해제한 뒤 **딱 한 번** 새로고침한다
(`sessionStorage.mixFix` 로 무한 새로고침 방지, 판이 맞으면 그 기록을 지운다).

**3차 방어 — 홈 그리기 실패 안내.**
`renderHome()` 을 try/catch 로 감쌌다. 실패해도 화면이 텅 비지 않고
"화면을 그리다 문제가 생겼어요 · 앱을 껐다 다시 열어주세요"와 **녹음은 안전하다**는 안내가 뜬다.

**검증(재현 테스트).** 세 가지 고장 조합을 실제로 만들어 확인했다 —
① 모양만 예전 것 ② 뼈대만 예전 것 ③ 대본만 예전 것 → **셋 다 스스로 복구(새로고침 1회, 무한루프 없음)**,
정상 조합은 복구가 헛돌지 않고 카드 6개가 그대로 나온다.

### ▶ v24 직후 긴급 수정 (v25) — ⚠️ 다시 겪지 않도록 꼭 읽을 것

**증상:** v24 배포 직후 민정 폰에서 **홈에 이야기가 하나도 안 보임**("다 사라졌어").

**원인 1 — 반쪽 섞임 (진짜 원인).**
서비스워커가 `fetch(req)` 로 받으면 **브라우저가 몰래 갖고 있던 사본**을 줄 수 있다.
GitHub Pages는 `max-age=600`(10분)을 붙이므로, 배포 직후엔
**index.html 은 예전 것 + app.js 는 새 것** 같은 조합이 생긴다.
새 app.js가 예전 뼈대에 없는 `#storyGrid` / `#recEntry` 를 찾다가 오류 →
이벤트 연결 줄에서 멈춰 **renderHome() 이 아예 실행 안 됨** → 목록 텅 빔.
- 조치 ①: `sw.js` 에서 **`fetch(req.url, { cache: "no-store" })`** 로 받는다. 절대 그냥 `fetch(req)` 로 되돌리지 말 것.
- 조치 ②: `app.js` 시작 때 **필수 요소가 하나라도 없으면** 캐시를 비우고 **딱 한 번** 새로고침(`recoverFromMixedVersion`,
  `sessionStorage.mixFix` 로 무한 새로고침 방지). 사용자가 아무것도 안 해도 스스로 낫는다.
- 📌 **교훈: index.html 의 id 를 바꾸는 변경은 언제든 이 섞임을 만든다.** 앞으로도 자가 복구를 유지할 것.

**원인 2 — 너무 흐렸다.**
`.gcard.todo { opacity: .3 }` 이라 밤 테마 위에서 거의 안 보였다. 게다가 홈에서 또렷한 건
**전 장면 녹음을 끝낸 이야기뿐**이라, 대부분 하다 만 상태면 화면이 텅 빈 것처럼 보인다.
- 조치: `opacity: .62` + 카드에 **`🎙 3 / 11`** 진행 표시(`.gtodo`)를 붙여
  "사라진 게 아니라 녹음 중"이 화면에 드러나게 했다.

### ▶ 전면 개편 이관 (v23) — 실제 앱에 적용됨
- **대본**: `scripts-data.js` 가 **장면 구조**(`scenes:[{emoji,lines}]`)로 바뀜. `lines` 통짜 구조는 없어짐.
- **저장**: 예전과 **같은 창고**(`voicebook` / `recordings`)를 쓰되 열쇠가 둘 —
  - 장면 클립 `id:v2:voice:장면번호` (지금 방식)
  - 예전 통 녹음 `id:voice` (옛 버전) — **지우지 않음**
- **예전 녹음 호환**: 장면 녹음이 없고 예전 통 녹음만 있으면 홈에 `예전 녹음` 이라 뜨고, 누르면 그 파일을 통째로 들려줌(그림은 표지 하나). 녹음 화면엔 "장면마다 다시 녹음하면 그림이 같이 넘어가요" 안내.
- **시범 녹음 가져오기**: 시범 페이지(`voicebook-demo` 창고)에 녹음이 있으면 **첫 실행 때 한 번** 실제 앱으로 복사(이미 있는 건 안 건드림). 시범 쪽 원본은 그대로.
- **백업 파일**: 지금은 `{kind:"scene-clips",version:3,clips:[...]}`. 복원은 **예전 형식(`recordings`)도 그대로 받음**.
- `story-demo.html` 은 이제 **역할 끝**(참고용으로만 남겨둠). 앞으로 수정은 실제 앱에서. (`sw.js` CACHE / `app.js` APP_VERSION 올리기). ⚠️ 교체 시 **index.html의 숨은 Netlify 폼(`<form name="feedback" netlify hidden>`)은 반드시 유지**해야 의견 폼이 감지됨. 기존 앱 데이터(`voicebook` DB)와 데모(`voicebook-demo`)는 store가 다름 → 교체 시 마이그레이션/안내 필요할지 검토.

**⭐ 셀링포인트(기록):** 부모끼리 **백업 파일 주고받기**로 엄마·아빠 목소리를 서로 합칠 수 있음(각자 녹음 안 해도 됨). 복원이 '합치기'라 자연스럽게 됨. → 마케팅/도움말에 강조.

**🎨 아이콘(보류, 방향만):** 현재(동화책 표지+초승달)가 '책 느낌 약함'. 검토된 후보 — **A) 펼친 동화책 + 위 초승달·별**(페이지 줄은 은은하게/빼기), **C) 두꺼운 하드커버 동화책 + 별**. 방식: `icons/gen_icons.py`(PIL, 4배 슈퍼샘플), 밤 네이비 배경 + 금/크림. 확정 후 재생성(아이폰은 삭제 후 재추가해야 반영). 앱 개편과 **독립**.

---

## 🔜 이어서 할 개선 작업 (피드백 라운드)

각 항목에 문제·손댈 파일·방법 후보·결정할 점을 적어 바로 이어서 작업할 수 있게 함.

### B1. 대본 문단·띄어쓰기 정리 (읽기 쉽게 끊어읽기) ⬅️ 우선순위 높음
- **문제:** 지금은 모든 줄이 `.line`에 **똑같은 간격**이라 장면 구분이 없어 "다 이어져" 보임. 어디서 끊어 읽을지 헷갈림.
- **원하는 것:** 장면(문단)별 간격 다르게, 대사/지문 묶기, 긴 문장을 읽기 좋은 호흡으로 다시 나누기.
- **손댈 곳:** `scripts-data.js`(대본 5편 재구성), 필요시 `style.css .line` / `app.js renderScriptBody`.
- **방법 후보:** ① 빈 줄/구분자로 문단 나누기(최소 변경), ② 각 이야기를 `paragraphs`(줄 배열의 배열) 구조로.
- **결정 필요:** 데이터 구조 변경(②) vs 최소 변경(①).

### B3. 녹음 중간 실수 → 처음부터 다시 하는 문제 개선
- **방식 후보 (미정):** ① 일시정지·이어녹음(MediaRecorder `pause()/resume()`, 쉬움) · ② 문단별 녹음 후 자동 이어붙이기(편하지만 큼, B1과 묶으면 자연스러움) · ③ 녹음 전 3초 카운트다운.
- **손댈 곳:** `app.js` 녹음 로직(`startRecording`/`onRecorderStop`/도크).

### B5. 저장 위치 파편화(인앱 브라우저) 대응 ⚠️ 크리티컬
- **문제:** 녹음은 IndexedDB에 **브라우저별로 따로** 저장. 카톡 인앱 브라우저에서 녹음 → 나중에 사파리로 열면 **안 보임**(웹앱 근본 한계).
- **지금 상태:** 인앱 브라우저면 "사파리로 열어주세요" 경고 모달(`app.js showIabNoticeIfNeeded`).
- **근본 해결책:** 아래 "클라우드 백업"(선택 로그인).
- **결정 필요:** 단기(경고 강화)만 vs 중기(클라우드 백업) 착수.

---

## 🔮 다음에 만들/다듬을 것 (급하지 않음)

- [ ] **동화 더 추가** (전래동화 등) — 원하는 이야기 말하면 클로드가 넣어줌.
  - 지금 6편(장면 수): 커다란 순무 11 · 빨간 모자 11 · 장갑 12 · 별을 세는 아기 곰 11 · 달님이 데려다준 꿈나라 11 · 달아난 블록들 13(창작·생활습관). **총 69장면**
  - 📦 **뺀 이야기(v29):** 아기돼지 삼형제(`three-pigs` 12장면) · 미운 아기 오리(`ugly-duckling` 10장면).
    민정 요청으로 교체. 대본은 git 기록에 남아 있으니 되살릴 수 있다.
    ⚠️ 예전 녹음은 **지우지 않았다** — 열쇠(`three-pigs:v2:...`)가 그대로 남아 백업 파일에도 담긴다.
    같은 id 로 다시 넣으면 그 녹음이 그대로 살아난다.
  - 📏 **기준: 한 편은 10장면 이상.** 그래야 듣는 재미가 있음(민정). 새 이야기 넣을 때도 이 기준으로.
  - ⚠️ **대본을 고치면 그 이야기는 다시 녹음해야 한다.** 녹음은 `이야기:장면번호`로 저장돼서, 장면을 넣거나 빼면 예전 녹음이 다른 장면에 붙는다. (홈에서 그 이야기 🎙 눌러 장면별로 다시 녹음)
  - **생활 습관 동화**가 반응 좋으면 더 만들 만함 (예: 밥 안 먹기, 이 닦기 싫어하기, 기다리기 힘들어하기).
- [ ] **클라우드 백업(선택 로그인)** — 녹음을 인터넷에 안전 보관해 폰 바꿔도 안 사라지게. 서버·비용 필요. **프리미엄(수익화)** 과 연결. (B5의 근본 해결책)
- [ ] **낮 버전 = 햇살책** — 낮에 보는 밝은 테마 + 활기찬 이야기 (자매품). 후보였던 이름: 낮잠책, 해뜰책.
- [ ] **백업 용어 다듬기** — "⬇ 내려받기"(오디오 사본, 복원 불가)와 "백업"이 이름이 비슷해 헷갈릴 여지.
- [ ] **수익화** — 반응 좋아지면 구독/동화팩 검토.

---

## 🗣 다음에 클로드와 **함께 논의할 것** (아직 결론 X)

> 새 세션에서 클로드는 여기를 보고, 아래 항목을 **민정과 상의**한 뒤 진행한다. (지금은 방법 확정 안 함)

- [x] ~~사용자 의견을 민정·클로드가 같이 보고 검토·반영하는 방법~~ → **정해짐 (아래 "의견 받기")**

## ✅ 이미 된 것 (참고)

- 앱 이름 **별밤책**, 밤 테마, 고운돋움 글씨, 화면 꽉 채우기(아이폰 홈 앱 포함).
- 동화 5편(아기돼지 삼형제·빨간 모자·미운 아기 오리·별을 세는 아기 곰·달님이 데려다준 꿈나라).
- 엄마/아빠 목소리 **녹음·저장·재생**(모두 이 기기 안에서만).
- **아기 이름** 넣기, **무드등 재생**(글자 없이 목소리), **랜덤 이어재생**.
- **도움말(?)**, **의견 보내기(앱 안 폼)**, **카톡 등 인앱 브라우저 안내**, **사용 통계**(GoatCounter, 익명).
- **한 목소리 · 아기 홈 개편 (v24):** 위 "한 목소리 · 아기 홈 개편" 참고.
- **개선 라운드 (v16~v21):**
  - 홈 화면 아이콘 **마이크 → 동화책 표지**(밤하늘 + 큰 초승달·별 + 페이지 결)
  - 오른쪽 위 달 **은은하게**, 빨간 모자 **"빵과 우유 → 따뜻한 죽"**
  - 홈 하단/의견 문구 정리(문장 안 잘리게), **'파일 불러오기' 버튼 제거**
  - **홈 정리 (v22, 실제 앱):** 흩어져 있던 **백업·도움말·의견 → `⚙️ 더보기` 한 창**(쭉 스크롤). **아기 이름은 제목 위로**("○○이의 별밤책", 눌러서 수정). 녹음이 있는데 백업을 한 번도 안 했으면 홈 아래에 **"🛟 아직 백업 전이에요"** 안내(백업하면 사라짐).
  - **백업 개편:** `.txt` 전환(카톡이 .json 거부) + 공유창 즉시 뜨게(gesture 버그 수정) + 카톡 '나에게' 중심 백업/복원 단계 안내 + 복원 `.txt` 지원 → *실기기 확인 필요*

---

## 💬 의견 받기 (수집 → 검토 → 반영)

**정해진 방식:** 앱 안의 예쁜 폼은 그대로 두고, 내용만 **구글 폼 → 구글 스프레드시트**로 보낸다.
클로드는 **구글 드라이브 연결로 그 시트를 직접 읽을 수 있다**(Gmail·Netlify는 못 읽음). 그래서 이 방식으로 정함.

```
사용자가 앱에서 의견 씀 → 구글 폼으로 전송 → 구글 시트에 한 줄씩 쌓임
   ├─ 민정: 새 의견 오면 메일 알림
   └─ 클로드: "이번 주 의견 정리해줘" 하면 시트를 읽어 분류·요약 → 뭘 고칠지 정하고 바로 수정
```

- **검토 주기:** 따로 자동 정리는 안 함. **민정이 물어볼 때만** 클로드가 시트를 읽고 정리한다.
**연결 완료 (v23):**
| | 값 |
|---|---|
| 폼 (편집) | https://docs.google.com/forms/d/1VIL0atTTjKzE8ObqvvyCHIU6N72DMRrPwu2sflKivWU/edit |
| 응답 시트 ⭐ | https://docs.google.com/spreadsheets/d/1-F7-ZHXF6O8wuZ_Qx6GP8UTMNCRjnK1LjanGwydOGNY/edit |
| 시트 열 | `타임스탬프` · `내용` · `이메일(선택)` |
| `내용` | `entry.532879545` |
| `이메일(선택)` | `entry.1819893667` |

> 클로드는 위 **응답 시트 주소**로 바로 읽는다. "이번 주 의견 정리해줘" 하면 그 시트를 읽어 정리한다.

- **코드 위치:** `app.js` 맨 위 `FEEDBACK_FORM = { id, message, email }`.
  - 비어 있으면 예전 Netlify 방식으로 보냄(= 지금 라이브에선 전송 안 됨).
  - 구글 폼은 다른 사이트 요청에 답을 안 줘서(no-cors) **성공 여부는 확인 불가** — 통신만 되면 보낸 것으로 본다.
  - 내용 끝에 `별밤책 v23 · 아이폰 · 홈화면 앱` 같은 **짧은 꼬리표**가 자동으로 붙는다(개인 식별 정보 아님).
- **`entry` 번호 찾는 법(클로드용):** 폼 링크를 WebFetch 로 읽어 `entry.숫자` 를 뽑는다. 안 되면 민정에게
  폼 편집화면 ⋮ → **"미리 채워진 링크 가져오기"** 로 만든 주소를 받아서 거기서 뽑는다.
- `index.html` 의 숨은 Netlify 폼(`<form name="feedback" netlify hidden>`)은 **되돌릴 때를 대비해 그대로 둔다.**

## 🔑 중요 정보 (주소·계정)

| 항목 | 값 |
|---|---|
| 현재 라이브(공유·확인용) | ⭐ **https://minjung9767-gif.github.io/voice-book/** (GitHub Pages) |
| 시범 페이지 | https://minjung9767-gif.github.io/voice-book/story-demo.html |
| (구)Netlify 주소 | https://voice-book.netlify.app/ — ⚠️ **크레딧 소진으로 배포 일시정지(v18에서 멈춤)**. 다음 결제주기 자동복구 or 유료. |
| 코드 저장소(GitHub) | minjung9767-gif/voice-book |
| 배포 | **main push마다 GitHub Pages 자동 배포**(`.github/workflows/deploy-pages.yml`). Pages는 저장소 Settings→Pages→Source=GitHub Actions로 켜져 있음. |
| 참고 저장소(디자인 원본) | minjung9767-gif/ginjoo-play-program (공개, 익명 clone 가능) |
| 통계 | https://voicebook.goatcounter.com (코드: voicebook) |
| 의견 수집 | 앱 안 폼 → Netlify Forms |

## 🛠 고칠 때 (참고)
- 클로드한테 고쳐달라고 하면, 코드 수정 → PR → 머지까지 하고 자동 배포됨.
- 앱을 크게 바꾸면 `app.js`의 `APP_VERSION` 과 `sw.js`의 `CACHE` 버전 숫자를 올림(온라인이면 폰에 바로 반영, network-first).
- 아이콘 다시 만들기: `cd icons && python3 gen_icons.py` (Pillow 필요). 아이폰은 이미 추가한 홈 화면 아이콘이 자동 갱신 안 됨 → 삭제 후 재추가.
- 저장 방식: 녹음은 브라우저 IndexedDB(기기 안에만). 서버 없음.
