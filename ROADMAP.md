# 별밤책 — 할 일 메모 📝

> 민정이 "나 뭐 해야 돼?" 하고 물으면, 클로드는 이 파일을 열어보고 **📌 민정이 할 일**부터 알려준다.
> (엄마·아빠 목소리로 읽어주는 아기 잠자리 그림책 앱)

---

## 📌 민정이 할 일 (지금 남은 것)

- [ ] **1. 새 버전(v23 · 전면 개편) 폰에서 확인** — 라이브 = **minjung9767-gif.github.io/voice-book/**. 홈 → 이야기 → **장면마다 녹음** → 다 하면 홈에서 눌러 **들려주기**(그림이 목소리 따라 넘어가는지). 예전에 녹음해 둔 게 있으면 `예전 녹음` 으로 뜨는지도. 아이폰 홈 화면 앱은 **삭제 후 재추가**해야 새 아이콘이 보임.
- [ ] **2. 백업 실기기 테스트** — 새 버전에서 **💬 카카오톡으로 백업 보내기** → 카톡 '나에게'에 `별밤책-백업.txt` 도착하는지, 그걸로 **복원**까지 되는지 확인. (이게 이번 핵심 수정)
- [ ] **3. 의견 폼 마무리 3가지** (구글 폼 연결은 끝남 ✅)
  - [ ] 폼 **이름 바꾸기**: `제목 없는 설문지` → **`별밤책 의견`** (드라이브에서 찾기 쉽게)
  - [ ] **이메일 알림 켜기**: 폼 → **응답** 탭 → 오른쪽 ⋮ → **새 응답에 대한 이메일 알림 받기**
  - [ ] **배포 후 실기기 테스트**: 앱에서 의견 하나 보내보고 → 구글 시트에 줄이 생기는지 확인 (안 생기면 클로드에게 말하기)
- [ ] **4. GoatCounter 이메일 인증** — 통계 계정(voicebook.goatcounter.com) 가입 때 온 인증 메일 링크 안 눌렀으면 눌러두기.
- [ ] **5. 주변 부모들한테 주소 공유하고 반응 보기** — 주소: **minjung9767-gif.github.io/voice-book/** (Netlify 복구 전까지 이 주소)
  - 카톡으로 보내면 받는 사람이 "사파리/크롬으로 열기" 하도록(앱이 안내해줌).

---

## 🎨 긴주 스타일 재디자인 (진행 중 — 시범 `story-demo.html`)

긴주 놀이터 '잠자리 동화' 화면 느낌을 별밤책에 이식 중. 시범 페이지에서 검증 후 본 앱 이관.
- 시범 주소: `minjung9767-gif.github.io/voice-book/story-demo.html`
- 참고 원본: `github.com/minjung9767-gif/ginjoo-play-program` (js/games/story.js, css/style.css)

### ▶ 새 세션에서 이어가기: **지금 상태 = "시범 완성 → 전면 개편 착수" 직전**
- 시범(`story-demo.html`)에서 **녹음+확인 화면**은 검증 끝. 다음은 **전면 개편**(아래 계획).
- 아이콘은 **보류**(방향만 정함, 맨 아래 참고).

**확정(시범 `story-demo.html`에서 검증 완료):**
- 밤 테마 + `Jua`/`Dongle` 폰트, 장면(이모지+문장) 구조.
- **대본 5편 전부 장면화 완료**(원문 그대로 + 이모지). 상단 **임시** 이야기 전환(‹ 제목 ›)으로 5편 오감.
- **장면별 클립 녹음**(엄마/아빠 각각, 아무 장면이나 다시). 저장: IndexedDB `voicebook-demo`(본 앱과 분리), 키 `storyId:v2:voice:scene`.
- **녹음 화면 = 부모 '녹음+확인' 단일 모드** (아기용 자동재생/들려주기는 이 화면에 **없음**):
  - 아래 가운데 **페이저(❮ n/N 장면 ❯)** — 녹음 중에도 유지(화살표만 비활성)
  - 그 위 **'🔊 녹음 확인'**(녹음된 장면일 때만; 재생↔멈추기 토글, 확인 중 ❯ 누르면 다음 녹음 이어듣기)
  - 맨 아래 **고정** '🔴 이 장면 녹음 / 다시 녹음'
  - 옆 화살표 제거 → **글 폭 넓힘**(문장 덜 쪼개짐, 글씨 크기 일정)

**본 앱 이관 시 반영 (긴주와 다르게):**
- **홈 버튼**: 긴주는 바깥 앱으로 나가지만, 별밤책은 단일 앱 → 홈 버튼은 **별밤책 메인(이야기 고르기) 화면으로**.
- **오른쪽 상단 음소거 버튼 제거** — 불필요.
- (참고) 긴주 최근 변경: 다음 이야기 버튼 삭제 · 이모지 톤다운 · '책장' 표현.
- ※ 긴주에 민정이 이슈로 적어둔 메모가 더 있으면, 이 세션은 voice-book만 접근 가능 → 그 내용은 채팅에 붙여주면 반영.

**➡️ 다음 단계: 전면 개편 (방식 = "시범을 키워서 → 완성되면 메인 교체")**
살아있는 `index.html`을 바로 갈아엎지 말고, **`story-demo.html`을 완전한 앱으로 키운 뒤** 다 되면 메인으로 교체. (안전 · 미리보기 유지 · 마지막 1회 교체)

빌드 순서:
1. ✅ **이야기 고르기(홈) — 세로 목록** *(완료: `story-demo.html`)*. picker는 별밤책이 단일 앱이라 그리드 대신 **세로 목록**으로 결정. 각 줄 = 이모지 표지 + 제목 + 엄마🌷/아빠🌰 녹음 진행(n/N). 이야기 누르면 녹음+확인 화면 진입, 화면 위 🏠 홈 버튼으로 목록 복귀(임시 '‹ 제목 ›' 전환은 제거).
2. ✅ **아기 자동재생(들려주기) 화면** *(완료: `story-demo.html`)*. **라우팅 규칙 확정:** 홈에서 이야기 탭 → 한 목소리라도 **전체(전 장면) 녹음 완료면 들려주기**, 아니면 녹음하기. 완성된 줄엔 오른쪽에 **🎙(녹음 고치기)** 따로 — 녹음 접근은 **홈에서만**, 아기 화면엔 녹음으로 가는 길 **전혀 없음**(실수로 못 날림). 들려주기 = 장면 클립 자동 이어재생 + 그림 동기화, 버튼 없음. 화면 탭 = 멈춤/이어보기, 멈추면 **🏠 목록·🔁 처음부터**(+엄마·아빠 둘 다 완성이면 목소리 전환)만. 끝나면 '다 읽었어요'.
   - **랜덤 재생 규칙(3·4단계에서 구현 시):** 이야기 **단위**로 섞기(엄마/아빠 조합 단위 ❌ — 같은 내용 목소리만 바꿔 연달아 나오면 안 됨), 한 번 나온 이야기는 한 바퀴 돌기 전엔 다시 안 나옴, 목소리는 고른 쪽으로 통일, **완성된 이야기만** 후보.
3. ✅ **백업/복원(장면 클립 포맷)** *(완료: `story-demo.html`)*. 홈 우상단 **🛟 백업·복원** → 모달. 모든 장면 클립 → base64 `.txt` 하나(`{app,kind:"scene-clips",version:2,clips:[{key,mime,data}]}`), 공유(카톡 '나에게'), 복원 = **'합치기'**(같은 키 덮어쓰고 없던 키 추가). **부모끼리 파일 주고받아 복원하면 엄마·아빠 목소리가 한 폰에 합쳐짐**(mom 복원→아빠 복원→both 확인). 아이폰 gesture 대응(모달 열 때 파일 미리 생성).
4. ✅ **아기 이름·도움말·의견 얹기** *(완료: `story-demo.html`)*. 홈 하단 버튼 줄 = **✏️ 아기 이름 · 🛟 백업 · ❔ 도움말 · 💬 의견**. 이름 넣으면 대본 `{이름아}`에 반영(받침 조사 자동). 홈 버튼(메인으로)은 녹음/들려주기 화면에 이미 있음. 음소거 버튼은 데모에 원래 없음(뺄 것 없음). ⚠️ **의견은 기존 앱과 동일하게 Netlify Forms(`POST /`, form-name=feedback)** — GitHub Pages(현재 라이브)에선 실제 전송 안 됨, **Netlify 복구 + 메인 교체 후** 동작.
5. ✅ **메인(`index.html`)으로 교체 완료 (v23)**. 홈 구성은 v22의 **⚙️ 더보기 + 제목 위 아기 이름**을 그대로 유지. 숨은 Netlify 폼도 유지. 아래 "전면 개편 이관" 참고.

### ▶ 전면 개편 이관 (v23) — 실제 앱에 적용됨
- **대본**: `scripts-data.js` 가 **장면 구조**(`scenes:[{emoji,lines}]`)로 바뀜. `lines` 통짜 구조는 없어짐.
- **저장**: 예전과 **같은 창고**(`voicebook` / `recordings`)를 쓰되 열쇠가 둘 —
  - 장면 클립 `id:v2:voice:장면번호` (지금 방식)
  - 예전 통 녹음 `id:voice` (옛 버전) — **지우지 않음**
- **예전 녹음 호환**: 장면 녹음이 없고 예전 통 녹음만 있으면 홈에 `예전 녹음` 이라 뜨고, 누르면 그 파일을 통째로 들려줌(그림은 표지 하나). 녹음 화면엔 "장면마다 다시 녹음하면 그림이 같이 넘어가요" 안내.
- **시범 녹음 가져오기**: 시범 페이지(`voicebook-demo` 창고)에 녹음이 있으면 **첫 실행 때 한 번** 실제 앱으로 복사(이미 있는 건 안 건드림). 시범 쪽 원본은 그대로.
- **백업 파일**: 지금은 `{kind:"scene-clips",version:3,clips:[...]}`. 복원은 **예전 형식(`recordings`)도 그대로 받음**.
- `story-demo.html` 은 이제 **역할 끝**(참고용으로만 남겨둠). 앞으로 수정은 실제 앱에서. (`sw.js` CACHE / `app.js` APP_VERSION 올리기). ⚠️ 교체 시 **index.html의 숨은 Netlify 폼(`<form name="feedback" netlify hidden>`)은 반드시 유지**해야 의견 폼이 감지됨. 기존 앱 데이터(`voicebook` DB)와 데모(`voicebook-demo`)는 store가 다름 → 교체 시 마이그레이션/안내 필요할지 검토.

**⭐ 셀링포인트(기록):** 부모끼리 **백업 파일 주고받기**로 엄마·아빠 목소리를 서로 합칠 수 있음(각자 녹음 안 해도 됨). 복원이 '합치기'라 자연스럽게 됨. → 마케팅/도움말에 강조.

**🎨 아이콘(보류, 방향만):** 현재(동화책 표지+초승달)가 '책 느낌 약함'. 검토된 후보 — **A) 펼친 동화책 + 위 초승달·별**(페이지 줄은 은은하게/빼기), **C) 두꺼운 하드커버 동화책 + 별**. 방식: `icons/gen_icons.py`(PIL, 4배 슈퍼샘플), 밤 네이비 배경 + 금/크림. 확정 후 재생성(아이폰은 삭제 후 재추가해야 반영). 앱 개편과 **독립**.

---

## 🔜 이어서 할 개선 작업 (피드백 라운드)

각 항목에 문제·손댈 파일·방법 후보·결정할 점을 적어 바로 이어서 작업할 수 있게 함.

### B1. 대본 문단·띄어쓰기 정리 (읽기 쉽게 끊어읽기) ⬅️ 우선순위 높음
- **문제:** 지금은 모든 줄이 `.line`에 **똑같은 간격**이라 장면 구분이 없어 "다 이어져" 보임. 어디서 끊어 읽을지 헷갈림.
- **원하는 것:** 장면(문단)별 간격 다르게, 대사/지문 묶기, 긴 문장을 읽기 좋은 호흡으로 다시 나누기.
- **손댈 곳:** `scripts-data.js`(대본 5편 재구성), 필요시 `style.css .line` / `app.js renderScriptBody`.
- **방법 후보:** ① 빈 줄/구분자로 문단 나누기(최소 변경), ② 각 이야기를 `paragraphs`(줄 배열의 배열) 구조로.
- **결정 필요:** 데이터 구조 변경(②) vs 최소 변경(①).

### B3. 녹음 중간 실수 → 처음부터 다시 하는 문제 개선
- **방식 후보 (미정):** ① 일시정지·이어녹음(MediaRecorder `pause()/resume()`, 쉬움) · ② 문단별 녹음 후 자동 이어붙이기(편하지만 큼, B1과 묶으면 자연스러움) · ③ 녹음 전 3초 카운트다운.
- **손댈 곳:** `app.js` 녹음 로직(`startRecording`/`onRecorderStop`/도크).

### B5. 저장 위치 파편화(인앱 브라우저) 대응 ⚠️ 크리티컬
- **문제:** 녹음은 IndexedDB에 **브라우저별로 따로** 저장. 카톡 인앱 브라우저에서 녹음 → 나중에 사파리로 열면 **안 보임**(웹앱 근본 한계).
- **지금 상태:** 인앱 브라우저면 "사파리로 열어주세요" 경고 모달(`app.js showIabNoticeIfNeeded`).
- **근본 해결책:** 아래 "클라우드 백업"(선택 로그인).
- **결정 필요:** 단기(경고 강화)만 vs 중기(클라우드 백업) 착수.

---

## 🔮 다음에 만들/다듬을 것 (급하지 않음)

- [ ] **동화 더 추가** (전래동화 등) — 원하는 이야기 말하면 클로드가 넣어줌.
  - 지금 6편(장면 수): 아기돼지 삼형제 12 · 빨간 모자 11 · 미운 아기 오리 10 · 별을 세는 아기 곰 11 · 달님이 데려다준 꿈나라 11 · 달아난 블록들 13(창작·생활습관). **총 68장면**
  - 📏 **기준: 한 편은 10장면 이상.** 그래야 듣는 재미가 있음(민정). 새 이야기 넣을 때도 이 기준으로.
  - ⚠️ **대본을 고치면 그 이야기는 다시 녹음해야 한다.** 녹음은 `이야기:장면번호`로 저장돼서, 장면을 넣거나 빼면 예전 녹음이 다른 장면에 붙는다. (홈에서 그 이야기 🎙 눌러 장면별로 다시 녹음)
  - **생활 습관 동화**가 반응 좋으면 더 만들 만함 (예: 밥 안 먹기, 이 닦기 싫어하기, 기다리기 힘들어하기).
- [ ] **클라우드 백업(선택 로그인)** — 녹음을 인터넷에 안전 보관해 폰 바꿔도 안 사라지게. 서버·비용 필요. **프리미엄(수익화)** 과 연결. (B5의 근본 해결책)
- [ ] **낮 버전 = 햇살책** — 낮에 보는 밝은 테마 + 활기찬 이야기 (자매품). 후보였던 이름: 낮잠책, 해뜰책.
- [ ] **백업 용어 다듬기** — "⬇ 내려받기"(오디오 사본, 복원 불가)와 "백업"이 이름이 비슷해 헷갈릴 여지.
- [ ] **수익화** — 반응 좋아지면 구독/동화팩 검토.

---

## 🗣 다음에 클로드와 **함께 논의할 것** (아직 결론 X)

> 새 세션에서 클로드는 여기를 보고, 아래 항목을 **민정과 상의**한 뒤 진행한다. (지금은 방법 확정 안 함)

- [x] ~~사용자 의견을 민정·클로드가 같이 보고 검토·반영하는 방법~~ → **정해짐 (아래 "의견 받기")**

## ✅ 이미 된 것 (참고)

- 앱 이름 **별밤책**, 밤 테마, 고운돋움 글씨, 화면 꽉 채우기(아이폰 홈 앱 포함).
- 동화 5편(아기돼지 삼형제·빨간 모자·미운 아기 오리·별을 세는 아기 곰·달님이 데려다준 꿈나라).
- 엄마/아빠 목소리 **녹음·저장·재생**(모두 이 기기 안에서만).
- **아기 이름** 넣기, **무드등 재생**(글자 없이 목소리), **랜덤 이어재생**.
- **도움말(?)**, **의견 보내기(앱 안 폼)**, **카톡 등 인앱 브라우저 안내**, **사용 통계**(GoatCounter, 익명).
- **개선 라운드 (v16~v21):**
  - 홈 화면 아이콘 **마이크 → 동화책 표지**(밤하늘 + 큰 초승달·별 + 페이지 결)
  - 오른쪽 위 달 **은은하게**, 빨간 모자 **"빵과 우유 → 따뜻한 죽"**
  - 홈 하단/의견 문구 정리(문장 안 잘리게), **'파일 불러오기' 버튼 제거**
  - **홈 정리 (v22, 실제 앱):** 흩어져 있던 **백업·도움말·의견 → `⚙️ 더보기` 한 창**(쭉 스크롤). **아기 이름은 제목 위로**("○○이의 별밤책", 눌러서 수정). 녹음이 있는데 백업을 한 번도 안 했으면 홈 아래에 **"🛟 아직 백업 전이에요"** 안내(백업하면 사라짐).
  - **백업 개편:** `.txt` 전환(카톡이 .json 거부) + 공유창 즉시 뜨게(gesture 버그 수정) + 카톡 '나에게' 중심 백업/복원 단계 안내 + 복원 `.txt` 지원 → *실기기 확인 필요*

---

## 💬 의견 받기 (수집 → 검토 → 반영)

**정해진 방식:** 앱 안의 예쁜 폼은 그대로 두고, 내용만 **구글 폼 → 구글 스프레드시트**로 보낸다.
클로드는 **구글 드라이브 연결로 그 시트를 직접 읽을 수 있다**(Gmail·Netlify는 못 읽음). 그래서 이 방식으로 정함.

```
사용자가 앱에서 의견 씀 → 구글 폼으로 전송 → 구글 시트에 한 줄씩 쌓임
   ├─ 민정: 새 의견 오면 메일 알림
   └─ 클로드: "이번 주 의견 정리해줘" 하면 시트를 읽어 분류·요약 → 뭘 고칠지 정하고 바로 수정
```

- **검토 주기:** 따로 자동 정리는 안 함. **민정이 물어볼 때만** 클로드가 시트를 읽고 정리한다.
**연결 완료 (v23):**
| | 값 |
|---|---|
| 폼 (편집) | https://docs.google.com/forms/d/1VIL0atTTjKzE8ObqvvyCHIU6N72DMRrPwu2sflKivWU/edit |
| 응답 시트 ⭐ | https://docs.google.com/spreadsheets/d/1-F7-ZHXF6O8wuZ_Qx6GP8UTMNCRjnK1LjanGwydOGNY/edit |
| 시트 열 | `타임스탬프` · `내용` · `이메일(선택)` |
| `내용` | `entry.532879545` |
| `이메일(선택)` | `entry.1819893667` |

> 클로드는 위 **응답 시트 주소**로 바로 읽는다. "이번 주 의견 정리해줘" 하면 그 시트를 읽어 정리한다.

- **코드 위치:** `app.js` 맨 위 `FEEDBACK_FORM = { id, message, email }`.
  - 비어 있으면 예전 Netlify 방식으로 보냄(= 지금 라이브에선 전송 안 됨).
  - 구글 폼은 다른 사이트 요청에 답을 안 줘서(no-cors) **성공 여부는 확인 불가** — 통신만 되면 보낸 것으로 본다.
  - 내용 끝에 `별밤책 v23 · 아이폰 · 홈화면 앱` 같은 **짧은 꼬리표**가 자동으로 붙는다(개인 식별 정보 아님).
- **`entry` 번호 찾는 법(클로드용):** 폼 링크를 WebFetch 로 읽어 `entry.숫자` 를 뽑는다. 안 되면 민정에게
  폼 편집화면 ⋮ → **"미리 채워진 링크 가져오기"** 로 만든 주소를 받아서 거기서 뽑는다.
- `index.html` 의 숨은 Netlify 폼(`<form name="feedback" netlify hidden>`)은 **되돌릴 때를 대비해 그대로 둔다.**

## 🔑 중요 정보 (주소·계정)

| 항목 | 값 |
|---|---|
| 현재 라이브(공유·확인용) | ⭐ **https://minjung9767-gif.github.io/voice-book/** (GitHub Pages) |
| 시범 페이지 | https://minjung9767-gif.github.io/voice-book/story-demo.html |
| (구)Netlify 주소 | https://voice-book.netlify.app/ — ⚠️ **크레딧 소진으로 배포 일시정지(v18에서 멈춤)**. 다음 결제주기 자동복구 or 유료. |
| 코드 저장소(GitHub) | minjung9767-gif/voice-book |
| 배포 | **main push마다 GitHub Pages 자동 배포**(`.github/workflows/deploy-pages.yml`). Pages는 저장소 Settings→Pages→Source=GitHub Actions로 켜져 있음. |
| 참고 저장소(디자인 원본) | minjung9767-gif/ginjoo-play-program (공개, 익명 clone 가능) |
| 통계 | https://voicebook.goatcounter.com (코드: voicebook) |
| 의견 수집 | 앱 안 폼 → Netlify Forms |

## 🛠 고칠 때 (참고)
- 클로드한테 고쳐달라고 하면, 코드 수정 → PR → 머지까지 하고 자동 배포됨.
- 앱을 크게 바꾸면 `app.js`의 `APP_VERSION` 과 `sw.js`의 `CACHE` 버전 숫자를 올림(온라인이면 폰에 바로 반영, network-first).
- 아이콘 다시 만들기: `cd icons && python3 gen_icons.py` (Pillow 필요). 아이폰은 이미 추가한 홈 화면 아이콘이 자동 갱신 안 됨 → 삭제 후 재추가.
- 저장 방식: 녹음은 브라우저 IndexedDB(기기 안에만). 서버 없음.
