# 별밤책 — 개선 작업 메모 (이어서 하기용)

사용자 피드백을 정리한 목록. **끝난 것 / 남은 것**을 나눠 적어, 다음에 바로 이어서 작업할 수 있게 한다.
(장기 아이디어는 `ROADMAP.md` 참고 — 여긴 "지금 이 피드백 라운드"의 작업들.)

_마지막 갱신: 2026-08-05 · 현재 버전 v18 · 작업 브랜치 `claude/app-issues-improvements-7tzrz2`_

---

## ✅ 이번에 끝낸 것

- **오른쪽 위 달(moon) 은은하게** — 크기·글로우·투명도 낮춤 (`style.css .moon`, v16)
- **빨간 모자 "빵과 우유" → "따뜻한 죽"** — 2곳 교체 (`scripts-data.js`, v16)
- **홈 하단 안내문** — 두 줄로 분리 + 첫 줄 작게, 문장 잘림 방지 (`index.html`, `style.css .privacy-note`, v16)
- **의견 보내기 문구 줄바꿈** + 모달 텍스트 `word-break: keep-all` (한 줄 중간 잘림 방지) (`app.js openFeedback`, `style.css`, v16)
- **'파일 불러오기' 버튼 제거** — 사진·비디오만 뜨고 실제 불러오기 안 되던 문제 (`app.js`, `index.html`, v16)
- **백업 복원 안내에 "카카오톡에서 가져오는 법" 한 줄 추가** (`app.js openBackup`, v16) — *부분 완료, 아래 B4 참고*
- **홈 화면 아이콘: 마이크 → 동화책 표지** (밤하늘 + 큰 초승달·별 + 페이지 결) (`icons/gen_icons.py` + png 4종, v18)

### 확인 결과 "이미 되어 있던" 것 (추가 작업 불필요)
- **홈 화면 추가 시 추천 이름** — `apple-mobile-web-app-title` / manifest `short_name` 이미 `"별밤책"`.
- **도움말에 홈 화면 추가 방법** — `app.js openHelp()` 안에 아이폰/안드로이드 안내 이미 있음.

---

## 🔜 남은 것 (다음에 이어서)

### B1. 대본 문단·띄어쓰기 정리 (읽기 쉽게 끊어읽기)  ⬅️ 우선순위 높음
- **문제:** 지금은 모든 줄이 `.line`에 **똑같은 16px 간격**이라 장면 구분이 없어 "다 이어져" 보임. 어디서 끊어 읽을지 헷갈림.
- **원하는 것:** 장면(문단)별로 간격 다르게, 대사/지문 묶기, 긴 문장을 읽기 좋은 호흡으로 다시 나누기.
- **손댈 곳:** `scripts-data.js`(대본 5편 전체 재구성), 필요시 `style.css .line`(문단 간격) / `app.js renderScriptBody`(문단 그룹 렌더).
- **방법 후보:** ① 빈 줄/구분자를 넣어 문단 나누기, ② 각 이야기를 `paragraphs`(줄 배열의 배열) 구조로 바꿔 문단 간격 크게, 문단 내 줄 간격 작게.
- **결정 필요:** 데이터 구조를 바꿀지(②) 최소 변경(①)으로 갈지.

### B3. 녹음 중간 실수 → 처음부터 다시 하는 문제 개선
- **문제:** 한 번에 통으로 녹음. 중간에 틀리면 처음부터.
- **방식 후보 (미정 — 사용자 "나중에 결정"):**
  - ① **일시정지·이어녹음** — MediaRecorder `pause()/resume()`. 구현 쉬움.
  - ② **문단별로 나눠 녹음 후 자동 이어붙이기** — 가장 편하지만 구현 큼. (B1과 묶으면 자연스러움)
  - ③ **녹음 전 3초 카운트다운** — 실수 자체를 줄임.
- **손댈 곳:** `app.js` 녹음 로직(`startRecording`/`onRecorderStop`/도크).
- **결정 필요:** ①~③ 중 무엇으로 갈지. (B1 문단화가 되면 ②가 매력적)

### B4. 백업 UX 용어·안내 정리
- **문제:** "⬇ 내려받기"(오디오 파일 사본)와 "🛟 녹음 백업"(전체 JSON, 복원 가능)이 이름이 비슷해 헷갈림.
  내려받기 파일은 **복원 버튼으로 못 되살림**(복원은 JSON만).
- **원하는 것:** 용어 구분 명확히, 복원 안내를 더 친절하게(가능하면 그림/단계).
- **손댈 곳:** `app.js`(도크 버튼 라벨 `downloadSaved`, `openBackup` 모달 문구).
- **참고:** 카톡에서 가져오는 법은 v16에서 한 줄 추가함 → 여기서 더 다듬기.

### B5. 저장 위치 파편화(인앱 브라우저) 대응  ⚠️ 크리티컬
- **문제:** 녹음은 IndexedDB에 **브라우저별로 따로** 저장. 카카오톡 인앱 브라우저에서 녹음 → 나중에 사파리로 열면 **안 보임.** (웹앱 근본 한계)
- **지금 상태:** 인앱 브라우저면 "사파리로 열어주세요" 경고 모달 표시(`app.js showIabNoticeIfNeeded`).
- **원하는 것:** 경고를 더 강하게/친절하게, 백업을 더 쉽게 유도.
- **근본 해결책:** **클라우드 백업(선택 로그인)** — `ROADMAP.md`에 있음. 서버·비용 필요, 프리미엄과 연결.
- **결정 필요:** 단기(경고 강화)만 할지, 중기(클라우드 백업) 착수할지.

---

## 메모
- 파일/asset 크게 바꾸면 `app.js APP_VERSION` 과 `sw.js CACHE` 버전 숫자 올리기(폰 반영용).
- 아이콘 다시 만들려면: `cd icons && python3 gen_icons.py` (Pillow 필요).
- 아이폰은 이미 홈 화면에 추가한 아이콘이 자동 갱신 안 됨 → 삭제 후 재추가해야 새 아이콘 반영.
